---
import { getLangFromUrl, useTranslations } from "@i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<div class="flex md:justify-center">
    <div class="mt-24 p-5 flex flex-col gap-1">
        <h1 class="text-8xl font-bold tracking-tight text-sky-700 dark:text-sky-500">
            Benjamin <br />Fabri
        </h1>
        <h2 class="text-2xl tracking-tight text-zinc-800 dark:text-zinc-200">
            {t("Développeur web full stack")}
        </h2>
    </div>
</div>
